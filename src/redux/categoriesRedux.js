// selectors

// action name creator

// action types

// action creators

// thunk creators

// reducer
export const categoriesReducer = (statePart = [], action = {}) => {
  switch (action.type) {
    default:
      return statePart;
  }
};
