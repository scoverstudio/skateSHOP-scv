// selectors
export const getAllProducts = (state) => state.products;

// action name creator

// action types

// action creators

// thunk creators

// reducer
export const productsReducer = (statePart = [], action = {}) => {
  switch (action.type) {
    default:
      return statePart;
  }
};
