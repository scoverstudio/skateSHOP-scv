// selectors
export const getAllCategoryNames = ({ categoryNames }) => categoryNames;
// action name creator

// action types

// action creators

// thunk creators
// reducer
export const categoryNamesReducer = (statePart = [], action = {}) => {
  switch (action.type) {
    default:
      return statePart;
  }
};
